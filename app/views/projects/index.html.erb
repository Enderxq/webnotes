<!DOCTYPE html>
<html>
<head>
<style>
table, td, th {
  border: 1px;
  line-height: 25px
}

th, td{
  text-align: center;
}

th {
  font-size: 15px;
}

table {
  width: 100%;
  border-collapse: collapse;
}
a:hover{
      background: #DCDCDC;
}
</style>
<link rel='stylesheet' href='https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css'>
</style>

<p id="notice"><%= notice %></p>

<h1>My Projects
  <a href=<%= new_project_path %>>
    <i class='bi bi-plus-circle-fill' style="font-size: 5rem; color: lightblue; position: static;"></i>
  </a>
</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Charge</th>
      <th>Sponsor</th> 
      <th>Team</th>
      <th>Operate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %> &nbsp; &nbsp; </td>
        <td><%= project.charge %></td>
        <td><%= project.sponsor %></td>
        <td><%= project.team_id %></td>
        <td> ddkk</td>
        <td><%= link_to 'Show', project %> &nbsp; |&nbsp;
            <%= link_to 'Edit', edit_project_path(project) %> &nbsp; |&nbsp;
            <%= link_to 'Destroy', project, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<!--
<%= link_to 'New Project', new_project_path %>
-->
